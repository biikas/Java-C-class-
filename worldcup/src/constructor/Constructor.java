package constructor;

public class Constructor {
	/**
	 * 1. name of constructor is same as the class name 
	 * 2. public, private ,protected , default can be used except static
	 * 3.constructor is not used for creating objects, it is used for initializing the objects
	 * 4.constructor is just like method,but is has no return type not even void 
	 * 5.types of constructor a. default constructor b. user defined constructor
	 * 6.Default constructor are created when a compiler search for constructor doesn't get one.
	 */
}
